# Let the Web Workspace publish plans and reviews

Plan and review edits remain drafts while they are being prepared, but explicit `Activate Plan` and `Publish Review` actions write directly to the Personal Learning Journal without requiring approval on iPhone. This makes the Web Workspace a real planning and reflection surface instead of a submission queue; device Calendar changes remain the exception and still require preview and confirmation on iPhone because they affect an external system.
