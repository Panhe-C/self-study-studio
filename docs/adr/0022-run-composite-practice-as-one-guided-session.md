# Run composite practice as one guided session

The iPhone uses a Guided Routine Player that starts and finishes one Practice Session while attributing active Practice Segments to the currently selected Block. Target durations prompt but never force transitions, learners may skip, reorder, revisit, or extend Blocks, pauses count nowhere, and the final summary combines repeated segments before one save operation.
