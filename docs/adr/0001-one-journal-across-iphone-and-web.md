# Use one personal learning journal across iPhone and Web

The Web Workspace is a second interaction surface over the same Personal Learning Journal as the iPhone App, with the existing private CloudKit data remaining canonical. We rejected a separate web database and account platform because it would introduce a second source of truth, require a new synchronization system, and expand a personal tool into a materially different multi-user product before that need exists.
